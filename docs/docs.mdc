# API Сервис расчета доставки - Уровень

## 🚀 Обзор

Сервис предоставляет упрощенный API для расчета стоимости доставки с **автоматической проверкой зон доставки**. Расчет стоимости основан на **формулах Excel** и учитывает реальные параметры транспорта, времени и тарифов. Теперь достаточно одного запроса с координатами и данными заказа - API сам проверит, входит ли адрес в зону доставки, и рассчитает стоимость.

## 🏗️ Архитектура

Сервис построен по модульной архитектуре с четким разделением ответственности:

```
src/
├── index.ts              # Точка входа приложения
├── middleware/           # Middleware для аутентификации
├── models/              # TypeScript интерфейсы и типы
├── routes/              # API маршруты
├── services/            # Бизнес-логика
└── utils/               # Утилиты (логгер)
```

## ✨ Ключевые особенности

- **🎯 Один запрос** - автоматическая проверка зоны + расчет доставки
- **📊 Формулы Excel** - точный расчет по реальным формулам из Excel
- **💰 Минимальная загрузка** - гарантированная рентабельность (74.3%)
- **🔄 Обратная совместимость** - старый API продолжает работать
- **⚡ Производительность** - меньше сетевых запросов
- **🛡️ Безопасность** - двухуровневая аутентификация
- **📊 Мониторинг** - подробное логирование всех операций

## 📋 Детальный анализ компонентов

### 1. **`index.ts` - Главный файл приложения**

```typescript
// Основные функции:
- Настройка Express сервера
- Подключение middleware (CORS, JSON парсинг)
- Настройка логирования запросов
- Регистрация API маршрутов
- Обработка ошибок
- Запуск сервера на порту 3000
```

**Ключевые особенности:**
- Автоматическое логирование всех HTTP запросов с временем выполнения
- Централизованная обработка ошибок
- CORS поддержка для кросс-доменных запросов
- Два основных API endpoint: `/api/calculate` и `/api/zone`

### 2. **`middleware/auth.ts` - Система аутентификации**

```typescript
// Три уровня защиты:
1. validateApiKey() - проверка API ключа в заголовке x-api-key
2. basicAuth() - Basic Authentication (логин/пароль)
3. apiProtection - комбинированный middleware [basicAuth, validateApiKey]
```

**Логика работы:**
- Сначала проверяется Basic Auth (если настроен)
- Затем проверяется API ключ
- Оба проверки должны пройти успешно
- Использует переменные окружения: `API_USERNAME`, `API_PASSWORD`, `API_KEY_CS_CART`

### 3. **`models/deliveryData.ts` - Типы данных**

```typescript
// Основные интерфейсы:
- Coordinates: { lat: number, lon: number }
- OrderInfo: { weight: number, cost: number, items?: number }
- DeliveryCalculationRequest: { lat, lon, order, zoneInfo? } // zoneInfo теперь опциональный!
- ZoneInfo: { inZone: boolean, zoneName?, error? }
- DeliveryCalculationResponse: { delivery_cost, delivery_time, options?, zoneInfo? }
- DeliveryZoneData: данные зоны из Supabase
```

**Особенности:**
- Все поля типизированы
- `zoneInfo` в запросе теперь опциональный - API проверяет зону автоматически
- Четкое разделение запросов и ответов
- Поддержка обратной совместимости

### 4. **`routes/calculation.ts` - API расчета доставки**

**Endpoint:** `POST /api/calculate`

**🆕 Новая логика работы:**
- **Автоматическая проверка зоны** - если `zoneInfo` не передан, API сам проверит зону доставки
- **Обратная совместимость** - если `zoneInfo` передан, повторная проверка не выполняется
- **Упрощенный запрос** - достаточно только координат и данных заказа

**Валидация запроса:**
```typescript
// Обязательные поля:
- lat, lon (координаты)
- order (вес и стоимость)

// Опциональные поля:
- zoneInfo (если не передан - проверяется автоматически)

// Валидация значений:
- lat: -90 до 90
- lon: -180 до 180  
- weight: > 0
- cost: >= 0
- zoneInfo.inZone: true (только если передан)
```

**Логика обработки:**
1. Получение данных из `req.body`
2. Валидация обязательных полей (lat, lon, order)
3. **Автоматическая проверка зоны** (если zoneInfo не передан):
   - Вызов `checkDeliveryZone()` для проверки координат
   - Если адрес вне зоны - возврат ошибки 400 с информацией о зоне
4. Вызов `calculateDelivery()` из сервиса
5. Добавление `zoneInfo` в ответ
6. Логирование результата
7. Возврат JSON ответа

### 5. **`routes/zone.ts` - API проверки зон**

**Endpoint:** `POST /api/zone`

**Валидация:**
```typescript
// Проверяет только координаты:
- lat: должно быть числом
- lon: должно быть числом
```

**Логика:**
1. Извлечение `lat, lon` из тела запроса
2. Валидация типов координат
3. Вызов `checkDeliveryZone()` из сервиса
4. Добавление timestamp в ответ
5. Логирование результата

### 6. **`services/calculationService.ts` - Бизнес-логика расчета**

**Алгоритм расчета стоимости (соответствует формулам Excel):**

```typescript
// Параметры транспорта:
CAPACITY_WEIGHT = 2000 кг        // Грузоподъемность машины
CAPACITY_VOLUME = 9 м³          // Объем кузова машины
AVERAGE_CITY_SPEED = 20 км/ч    // Средняя скорость по городу

// Тарифные параметры:
HOURLY_RATE = 850 руб/час       // Ставка за час работы
LOADING_TIME = 0.5 часа         // Максимальное время погрузки/разгрузки
CITY_DISTANCE = 7 км            // Среднее маршрутное расстояние по городу
MIN_LOAD_PERCENTAGE = 74.3%     // Минимальный процент загрузки
ADDITIONAL_COSTS = 100 руб      // Дополнительные расходы
MARGIN_PERCENTAGE = 10%         // Маржинальность доставки
EXPRESS_COEFFICIENT = 14.25%    // Коэффициент экспресс доставки
```

**Пошаговый расчет (формулы Excel):**

```typescript
// B2: Реальная доля загрузки = MAX(вес/грузоподъемность, объем/объем кузова)
loadRatio = Math.max(weight/2000, volume/9)

// B3: Необходимое количество рейсов (округление вверх)
requiredTrips = Math.ceil(loadRatio)

// B4: Количество дополнительных рейсов
additionalTrips = requiredTrips - 1

// B5: Эффективная загрузка = MAX(B2, минимальный%)
effectiveLoadRatio = Math.max(loadRatio, 74.3/100)

// B6: Время погрузки/разгрузки = 0.5 часа × B5
loadingTime = 0.5 * effectiveLoadRatio

// B7: Среднее время доставки = 7 км ÷ 20 км/ч = 0.35 часа
avgDeliveryTime = 7 / 20

// B8: Время доставки с учетом загрузки = B7 × B5
deliveryTime = avgDeliveryTime * effectiveLoadRatio

// B9: Себестоимость = (B6 + B8) × 850 руб/час + 100 руб
baseCost = (loadingTime + deliveryTime) * 850 + 100

// B10: Обычная доставка = B9 × 1.1, округление до 100 руб
regularCost = Math.round(baseCost * 1.1 / 100) * 100

// B11: Экспресс себестоимость = B9 × 1.1425
expressCostPrice = baseCost * 1.1425

// B12: Экспресс доставка = B11 × 1.1, округление до 100 руб
expressCost = Math.round(expressCostPrice * 1.1 / 100) * 100
```

**Особенности:**
- Точное соответствие формулам Excel из файла `app-formulabot-com-20250923055808.csv`
- Минимальная загрузка 74.3% обеспечивает рентабельность для малых грузов
- Округление до ближайших 100 рублей для удобства клиентов
- Время доставки зависит от количества рейсов: 1-2 дня или N-(N+1) дня

### 7. **`services/zoneService.ts` - Интеграция с Supabase**

**Функция:** `checkDeliveryZone(coordinates: Coordinates)`

**Логика работы:**
1. Подключение к Supabase через RPC функцию `is_in_delivery_zone`
2. Передача координат в базу данных
3. Получение массива зон доставки
4. Анализ результата:
   - Пустой массив = вне зоны
   - Есть данные = в зоне (берется первая зона)

**Обработка ошибок:**
- Ошибки Supabase логируются с деталями
- Возвращается понятное сообщение об ошибке
- Graceful fallback при сбоях

### 8. **`utils/logger.ts` - Система логирования**

**Возможности:**
```typescript
// Уровни логирования:
ERROR, WARN, INFO, DEBUG

// Настройки через переменные окружения:
LOG_DIR, LOG_FILE, LOG_TO_CONSOLE, LOG_LEVEL

// Функции:
- Запись в файл (logs/app.log)
- Вывод в консоль с цветами
- Форматирование с timestamp
- Фильтрация по уровню
```

**Особенности:**
- Автоматическое создание директории логов
- Цветной вывод в консоль
- Синхронная запись в файл
- Настраиваемый минимальный уровень логирования

## 🔄 Поток выполнения запросов

### 🆕 Новый способ - Расчет доставки с автоматической проверкой зоны:

```
1. HTTP POST /api/calculate
2. Middleware: basicAuth + validateApiKey
3. Валидация: lat, lon, order (zoneInfo опционально)
4. Автоматическая проверка зоны (если zoneInfo не передан):
   - Вызов checkDeliveryZone() для проверки координат
   - Если вне зоны: возврат ошибки 400 с zoneInfo
5. Сервис: calculateDelivery()
   - Расчет доли загрузки по весу и объему
   - Определение количества рейсов
   - Расчет времени погрузки и доставки
   - Применение тарифов и минимальной загрузки
   - Добавление маржи и округление до 100 руб
6. Добавление zoneInfo в ответ
7. Логирование результата
8. Возврат JSON ответа
```

### 🔄 Старый способ - Расчет доставки с предварительной проверкой зоны:

```
1. HTTP POST /api/calculate
2. Middleware: basicAuth + validateApiKey
3. Валидация: lat, lon, order, zoneInfo
4. Сервис: calculateDelivery()
   - Расчет доли загрузки по весу и объему
   - Определение количества рейсов
   - Расчет времени погрузки и доставки
   - Применение тарифов и минимальной загрузки
   - Добавление маржи и округление до 100 руб
5. Добавление zoneInfo в ответ
6. Логирование результата
7. Возврат JSON ответа
```

### Запрос проверки зоны (отдельный endpoint):

```
1. HTTP POST /api/zone  
2. Middleware: basicAuth + validateApiKey
3. Валидация: lat, lon (числа)
4. Сервис: checkDeliveryZone()
   - RPC запрос к Supabase
   - Анализ результата
5. Добавление timestamp
6. Логирование результата
7. Возврат JSON ответа
```

## 🛡️ Безопасность

- **Двухуровневая аутентификация:** Basic Auth + API Key
- **Валидация входных данных:** все параметры проверяются
- **Обработка ошибок:** централизованная с логированием
- **CORS:** поддержка кросс-доменных запросов

## �� Мониторинг и логирование

- **HTTP запросы:** автоматическое логирование с временем выполнения
- **Бизнес-логика:** детальное логирование расчетов
- **Ошибки:** полное логирование с stack trace
- **Файлы логов:** `logs/app.log` с ротацией

## 📝 Примеры использования API

### 🆕 Новый способ (рекомендуемый):

```bash
# Расчет доставки с автоматической проверкой зоны
curl -X POST http://localhost:3000/api/calculate \
  -H "Content-Type: application/json" \
  -H "x-api-key: your-api-key" \
  -H "Authorization: Basic $(echo -n 'username:password' | base64)" \
  -d '{
    "lat": 48.480223,
    "lon": 135.071917,
    "order": {
      "weight": 2.5,
      "cost": 3000
    }
  }'
```

**Ответ (успех):**
```json
{
  "delivery_cost": 700,
  "delivery_time": "1-2 дня",
  "express_delivery_cost": 800,
  "options": [
    {
      "name": "Экспресс доставка",
      "cost": 800,
      "description": "Приоритетная доставка"
    }
  ],
  "zoneInfo": {
    "inZone": true,
    "zoneName": "Зона доставки — Хабаровск"
  }
}
```

**Ответ (адрес вне зоны):**
```json
{
  "error": true,
  "message": "Адрес находится вне зоны доставки",
  "zoneInfo": {
    "inZone": false,
    "error": "Адрес находится вне зоны доставки"
  }
}
```

### 🔄 Старый способ (обратная совместимость):

```bash
# Расчет доставки с предварительно проверенной зоной
curl -X POST http://localhost:3000/api/calculate \
  -H "Content-Type: application/json" \
  -H "x-api-key: your-api-key" \
  -H "Authorization: Basic $(echo -n 'username:password' | base64)" \
  -d '{
    "lat": 48.480223,
    "lon": 135.071917,
    "order": {
      "weight": 2.5,
      "cost": 3000
    },
    "zoneInfo": {
      "inZone": true,
      "zoneName": "Хабаровск"
    }
  }'
```

### 🔍 Проверка зоны (отдельный endpoint):

```bash
# Проверка зоны доставки
curl -X POST http://localhost:3000/api/zone \
  -H "Content-Type: application/json" \
  -H "x-api-key: your-api-key" \
  -H "Authorization: Basic $(echo -n 'username:password' | base64)" \
  -d '{
    "lat": 48.480223,
    "lon": 135.071917
  }'
```

## 🧪 Тестирование

- **Unit тесты:** для сервисов и middleware
- **API тесты:** для endpoints с новой логикой автоматической проверки зон
- **Интеграционные тесты:** для Supabase (опционально)
- **Моки:** для изоляции тестов
- **Покрытие:** полное покрытие новой функциональности

## ✅ Преимущества обновленного API

1. **🎯 Упрощение** - один запрос вместо двух
2. **📊 Точность** - расчет по реальным формулам Excel
3. **💰 Рентабельность** - минимальная загрузка 74.3% для малых грузов
4. **⚡ Производительность** - меньше сетевых запросов
5. **🔄 Совместимость** - старый код продолжает работать
6. **🛡️ Надежность** - централизованная логика проверки зон
7. **📊 Мониторинг** - подробное логирование всех операций

## 📊 Примеры расчетов

### Малый груз (0.1 кг, 0.1 м³):
- Доля загрузки: 0.00011 (очень мало)
- С учётом минимума: 0.743 (74.3%)
- Себестоимость: ~636 руб
- **Обычная доставка: 700 руб**
- **Экспресс доставка: 800 руб**

### Большой груз (1500 кг, 6 м³):
- Доля загрузки: 0.75 (75% по весу)
- Количество рейсов: 1
- Эффективная загрузка: 0.75
- **Обычная доставка: 800 руб**
- **Экспресс доставка: 900 руб**

Этот сервис представляет собой хорошо структурированный, масштабируемый API для расчета доставки с **автоматической проверкой зон** и **точным расчетом по формулам Excel**.

